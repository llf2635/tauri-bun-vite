# .env.test 文件是测试环境的配置文件，用于代码测试和调试，不建议在生产环境中使用。
# 测试环境基础配置，值不用加引号，直接写值即可

# Vue的mode配置
MODE_ENV=test

# vite构建的应用相关配置，必须以VITE_打头才能暴露出去
# 环境名称
VITE_APP_ENV_TITLE=测试环境

# 测试环境，单元测试/端到端测试配置

# 测试环境 API 地址（可指向测试服务器）
VITE_API_BASE_URL=https://api.test.yourdomain.com

# 启用 Mock 数据
VITE_USE_MOCK=true

# 控制日志输出等级（便于调试测试代码）
VITE_LOG_LEVEL=debug

# 第三方服务测试 Token（仅限测试环境）
VITE_MAP_API_KEY=test_xxx123456

# 是否启用慢速网络模拟（用于 UI 测试）
VITE_SIMULATE_SLOW_NETWORK=true

# 埋点是否上报到测试统计平台
VITE_ANALYTICS_ENDPOINT=https://log.test.analytics.com