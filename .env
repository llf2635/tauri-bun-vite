# 默认的环境变量文件，适用于所有环境，包括开发环境、测试环境和生产环境。在该文件中定义的变量对整个项目的所有环境都起作用。
# 一般定义一些通用的环境变量，如数据库连接信息、日志路径、应用名称、运行端口等。其他的环境文件（如 dev test prod）共享这些变量，但可以根据需要进行定制。

# 所有环境，基础默认配置

# ======================
# 应用基础配置
# ======================

# 应用名称 (必填)
VITE_APP_NAME=MyAdminSystem

# 应用版本 (从package.json获取)
VITE_APP_VERSION=1.0.0

# 默认语言
VITE_APP_LOCALE=zh-CN

# 是否开启调试模式
VITE_DEBUG=false

# ======================
# API 配置
# ======================

# API基础地址 (开发环境通常配代理)
VITE_API_BASE_URL=/api

# API请求超时(毫秒)
VITE_API_TIMEOUT=10000

# WebSocket地址
VITE_WS_URL=wss://ws.example.com

# ======================
# 路由配置
# ======================

# 基础路由路径 (部署在子目录时需要)
VITE_BASE_ROUTE=/

# 是否使用Hash路由模式
VITE_USE_HASH_ROUTER=false

# ======================
# 第三方服务
# ======================

# Google Analytics ID (可选)
# VITE_GA_MEASUREMENT_ID=G-XXXXXX

# Sentry DSN (可选)
# VITE_SENTRY_DSN=https://xxxx@sentry.io/xxx

# 地图服务密钥 (可选)
# VITE_MAP_API_KEY=AIzaSyBk...

# ======================
# 功能开关
# ======================

# 是否启用Mock数据
VITE_ENABLE_MOCK=false

# 是否开启性能监控
VITE_ENABLE_PERF_MONITOR=false

# 是否开启页面水印
VITE_ENABLE_WATERMARK=true

# ======================
# 业务特定配置
# ======================

# 列表默认分页大小
VITE_PAGE_SIZE=20

# 最大上传文件大小(MB)
VITE_MAX_UPLOAD_SIZE=50

# 会话超时时间(分钟)
VITE_SESSION_TIMEOUT=30