# .env.production 文件是生产环境的配置文件，一般来说，生产环境的配置项都比较少，只有一些基础的配置项，比如应用的名称、域名、接口地址等。
# 生产环境基础配置，值不用加引号，直接写值即可
# 生产环境：只在打包时使用，在开发环境中不会生效

# Vue的mode配置
MODE_ENV=production

# vite构建的应用相关配置，必须以VITE_打头才能暴露出去
# 环境名称
VITE_APP_ENV_TITLE=生产环境

# 生产环境，生产环境专有配置

# 线上真实 API 地址
VITE_API_BASE_URL=https://api.yourdomain.com

# 禁用 Mock（生产环境不使用）
VITE_USE_MOCK=false

# 是否启用性能监控或埋点（如 Sentry、Datadog、友盟等）
VITE_ENABLE_MONITOR=true

# 第三方统计 ID（仅限生产使用）
VITE_ANALYTICS_ID=ga-123456789

# 是否开启调试日志（生产环境一般关闭）
VITE_ENABLE_DEBUG_LOG=false

# CDN 静态资源地址（可选）
VITE_CDN_URL=https://cdn.yourdomain.com/assets/

# 多语言国际化打包策略（是否压缩语言包）
VITE_I18N_COMPRESSION=false

# 构建版本号（可配合 CI/CD 注入）
VITE_APP_VERSION=1.0.0