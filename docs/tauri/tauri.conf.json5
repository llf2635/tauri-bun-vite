{
  // 约束下面的配置内容和语法遵循 Tauri 2 版本
  "$schema": "https://schema.tauri.app/config/2",
  // 应用名称
  "productName": "tauri-app",
  // 应用程序版本。如果删除，则使用 Cargo.toml 中的版本号。建议在 Tauri 配置中管理应用版本控制。
  "version": "0.1.0",
  // 应用唯一标识
  // 反向域名表示法的应用程序标识符 （e.g. com.tauri.example）。
  // 此字符串在应用程序中必须是唯一的，因为它用于系统配置，例如捆绑包 ID 和 webview 数据目录的路径。此字符串只能包含字母数字字符（A-Z、a-z 和 0-9）、连字符 （-） 和句点 （.）。。
  "identifier": "com.lcqh.tauri-app",
  // Tauri 构建配置，https://tauri.app/zh-cn/reference/config/#build
  "build": {
    // 在 bun run tauri dev 启动之前运行的 shell 命令
    "beforeDevCommand": "bun run dev",
    // 在 bun run tauri build 启动之前运行的 shell 命令
    "beforeBuildCommand": "bun run build",
    // 要在开发中加载的 URL，这通常是开发服务器的 URL，它通过热重载和 HMR 为您的应用程序资产提供服务。
    // 大多数现代 JavaScript 打包器（如 Vite）都提供了一种默认启动开发服务器的方法。
    "devUrl": "http://localhost:1420",
    // 应用程序前端打包资产的路径（通常是 javascript 捆绑器的 dist 文件夹）或 URL，
    // 该 URL 可以是在 tauri 应用程序中注册的自定义协议（例如：myprotocol://）或远程 URL（例如：https://site.com/app）。
    "frontendDist": "../dist"
  },
  // Tauri 应用 APP 配置，https://tauri.app/zh-cn/reference/config/#app
  "app": {
    // 应用窗口配置。https://tauri.app/zh-cn/reference/config/#windows
    "windows": [
      {
        // 窗口标题。默认值：“Tauri App”
        "title": "tauri-app",
        "width": 800,
        "height": 600,
        // 窗口是否居中开始
        "center": true,
      }
    ],
    // 应用安全配置。https://tauri.app/zh-cn/reference/config/#security
    "security": {
      "csp": null
    }
  },
  // Tauri 捆绑包配置，https://tauri.app/zh-cn/reference/config/#bundle
  "bundle": {
    // Tauri 是应该捆绑您的应用程序还是仅输出可执行文件。
    "active": true,
    // 捆绑目标目前支持 [“deb”、“rpm”、“appimage”、“nsis”、“msi”、“app”、“dmg”] 或 “all”。
    // “deb” debian 捆绑包 （.deb）
    // “rpm” RPM 捆绑包 （.rpm）
    // “appimage” AppImage 捆绑包 （.appimage）
    // “msi” Microsoft安装程序捆绑包 （.msi）
    // “nsis” NSIS 捆绑包 （.exe）
    // “app” macOS 应用程序包 （.app）
    // “dmg” Apple 磁盘映像包 （.dmg）
    "targets": ["deb","rpm", "dmg", "nsis", "msi", "app"],
    // 应用程序的发布者。默认为 identifier 标识符字符串中的第二个元素。例如：lcqh
    //当前映射到 Windows Installer 的 Manufacturer 属性和 debian 包的 Maintainer 字段，如果 Cargo.toml 没有 authors 字段。
    "publisher": "龙茶清欢",
    // 与应用程序关联的版权字符串。
    "copyright": "Copyright © 2022-2023 龙茶清欢(www.llf.com).All Rights Reserved",
    // 应用程序类型。应为以下内容之一：商业、开发工具、教育、娱乐、金融、游戏、图形和设计、医疗保健和健身、生活方式、医疗、音乐、新闻、摄影、生产力、参考、社交网络、体育、旅行、实用、视频，天气。
    "category": "Utility",
    // 应用程序的简短描述。
    "shortDescription": "应用程序简短描述",
    // 应用程序的更长、多行描述。
    "longDescription": "龙茶清欢的拥有者整的好帅啊！",
    // 要捆绑的应用资源。每个资源都是文件或目录的路径。支持 Glob 模式。https://tauri.app/zh-cn/develop/configuration-files/
    // 可以将平台特定配置文件存放在该指定目录下，https://tauri.app/zh-cn/reference/config/#bundleresources
    "resources": [
      "./resources"
    ]
    // 应用的图标，*.png 用于 Linux、icon.icns 用于 macOS、icon.ico 用于 Windows
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ],
    // Tauri 插件配置，https://tauri.app/zh-cn/reference/config/#plugins
    //    plugins: {
    //      // 应用更新插件配置，https://tauri.app/zh-cn/plugin/updater/
    //      updater: {
    //        pubkey: 'updater pub key',
    //        endpoints: [
    //          'https://my.app.updater/{{target}}/{{current_version}}',
    //          // "https://github.com/mike2635/blog-cloud/releases/download/{{target}}/{{current_version}}"
    //        ],
    //      },
    //    }
  }
}
