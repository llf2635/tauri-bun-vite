# .env.development 文件是开发环境的配置文件，在开发环境下，该文件中的配置项会覆盖默认配置项， 例如： 当我们执行 npm run dev 时，会读取该文件中的配置项，而不会读取默认配置项。
# 开发环境基础配置，值不用加引号，直接写值即可
# 开发环境：本地只启动前端项目，依赖开发环境（后端、APP）的接口，数据库等，不用考虑性能、安全等问题。

# Vue的mode配置
MODE_ENV=development

# vite构建的应用相关配置，必须以VITE_打头才能暴露出去
# 环境名称
VITE_APP_ENV_TITLE=开发环境

# 开发环境，开发专用配置

# 开发环境 API 地址
VITE_API_BASE_URL=http://localhost:3000/api

# 是否启用 Mock 数据（配合 mock.js 或 vite-plugin-mock）
VITE_USE_MOCK=true

# WebSocket 地址
VITE_WS_URL=ws://localhost:8080

# 调试日志开关
VITE_ENABLE_DEBUG_LOG=true

# 第三方服务密钥（仅限开发环境测试用）
VITE_MAP_API_KEY=dev_xxx123456

# 本地代理设置（在 vite.config.ts 中使用）
# 不需要写在这里，而是在 vite.config.ts 的 server.proxy 配置中引用